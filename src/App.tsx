import React from 'react';

interface AppProps {}

const App: React.FC<AppProps> = () => (
  <div className="App">
    <header className="App-header">
      App Component
    </header>
  </div>
);

export default App;
